import React from 'react';
import {DefaultSectionT, SectionListRenderItemInfo, Text} from 'react-native';

const AgendaItem = (info: SectionListRenderItemInfo<any, DefaultSectionT>) => {
  return <Text>ok</Text>;
};

export default AgendaItem;
